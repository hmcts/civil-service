package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code SET_LIP_RESPONDENT_RESPONSE_DEADLINE} for case type {@code CIVIL}.
 */
@Component
public class SETLIPRESPONDENTRESPONSEDEADLINE implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String SET_LIP_RESPONDENT_RESPONSE_DEADLINE = "SET_LIP_RESPONDENT_RESPONSE_DEADLINE";

    /**
     * Registers event {@code SET_LIP_RESPONDENT_RESPONSE_DEADLINE}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        builder.event(SET_LIP_RESPONDENT_RESPONSE_DEADLINE)
            .forAllStates()
            .name("Set LIP Response Deadline")
            .description("Set LIP Respondent Response deadline")
            .showSummary(false)
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_CIVIL_SYSTEMUPDATE)
            .grant(Set.of(Permission.R), UserRole.CITIZEN_CLAIMANT_PROFILE);
    }
}
