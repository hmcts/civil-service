package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code CUI_UPLOAD_MEDIATION_DOCUMENTS} for case type {@code CIVIL}.
 */
@Component
public class CUIUPLOADMEDIATIONDOCUMENTS implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String CUI_UPLOAD_MEDIATION_DOCUMENTS = "CUI_UPLOAD_MEDIATION_DOCUMENTS";

    /**
     * Registers event {@code CUI_UPLOAD_MEDIATION_DOCUMENTS}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        builder.event(CUI_UPLOAD_MEDIATION_DOCUMENTS)
            .forStates(CaseState.JUDICIAL_REFERRAL, CaseState.CASE_PROGRESSION, CaseState.HEARING_READINESS, CaseState.PREPARE_FOR_HEARING_CONDUCT_HEARING)
            .name("Cui Upload mediation documents")
            .description("CUI Upload mediation documents this event is only for CUI application")
            .displayOrder(24)
            .showCondition("responseClaimTrack=\"SMALL_CLAIM\" AND CaseAccessCategory=\"SPEC_CLAIM\"")
            .showSummary()
            .showEventNotes()
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CITIZEN_CLAIMANT_PROFILE, UserRole.CITIZEN_DEFENDANT_PROFILE)
            .grant(Set.of(Permission.R), UserRole.APP_SOL_SPEC_PROFILE, UserRole.RES_SOL_ONE_SPEC_PROFILE, UserRole.RES_SOL_TWO_SPEC_PROFILE);
    }
}
