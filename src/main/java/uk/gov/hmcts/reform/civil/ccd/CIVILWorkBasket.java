package uk.gov.hmcts.reform.civil.ccd;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>The work-basket input and result fields for {@code CIVIL}.
 */
@Component
public class CIVILWorkBasket implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * Configures the work-basket input and result fields for {@code CIVIL}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        builder.workBasketInputFields()
            .field("CaseAccessCategory", "Claim Type")
            .field("[CASE_REFERENCE]", "CCD Case Number")
            .field("legacyCaseReference", "CaseMan reference number");
        builder.workBasketResultFields()
            .field("[CASE_REFERENCE]", "CCD Case Number")
            .field("[STATE]", "State")
            .field("legacyCaseReference", "CaseMan reference number")
            .field("issueDate", "Date claim issued")
            .field("solicitorReferences", "Claimant Reference", f -> f.listElementCode("applicantSolicitor1Reference"))
            .field("caseListDisplayDefendantSolicitorReferences", "Defendant Reference(s)")
            .field("allPartyNames", "Party Names")
            .field("nextDeadline", "Next Deadline");
    }
}
