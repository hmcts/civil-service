package uk.gov.hmcts.reform.civil.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code REMOVE_DOCUMENT} for case type {@code CIVIL}.
 */
@Component
public class REMOVEDOCUMENT implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String REMOVE_DOCUMENT = "REMOVE_DOCUMENT";

    /**
     * Registers event {@code REMOVE_DOCUMENT}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        var fields = builder.event(REMOVE_DOCUMENT)
            .forAllStates()
            .name("Remove document")
            .description("Remove document/s")
            .displayOrder(1)
            .showSummary()
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_CIVIL_DOC_REMOVAL)
            .fields();
        fields.page("1");
        fields.readonlyNoSummary(CaseData::getDocumentToKeepCollection);
    }
}
