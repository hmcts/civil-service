package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.EnumSet;
import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.ccd.event.page.LIPCLAIMSETTLEDWorkAllocationIntegrationFieldsPage;
import uk.gov.hmcts.reform.civil.ccd.event.page.LIPCLAIMSETTLEDWorkAllocationPage;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code LIP_CLAIM_SETTLED} for case type {@code CIVIL}.
 */
@Component
public class LIPCLAIMSETTLED implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String LIP_CLAIM_SETTLED = "LIP_CLAIM_SETTLED";

    /**
     * Registers event {@code LIP_CLAIM_SETTLED}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        var fields = builder.event(LIP_CLAIM_SETTLED)
            .forStateTransition(EnumSet.allOf(CaseState.class), CaseState.CASE_SETTLED)
            .name("Claim is settle")
            .description("Claim settle response from Citizen Claimant")
            .showSummary()
            .endButtonLabel("Submit")
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CITIZEN_CLAIMANT_PROFILE)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_CIVIL_ADMIN, UserRole.CUI_ADMIN_PROFILE, UserRole.JUDGE_PROFILE, UserRole.RES_SOL_ONE_SPEC_PROFILE)
            .fields();
        LIPCLAIMSETTLEDWorkAllocationPage.apply(fields);
        LIPCLAIMSETTLEDWorkAllocationIntegrationFieldsPage.apply(fields);
    }
}
