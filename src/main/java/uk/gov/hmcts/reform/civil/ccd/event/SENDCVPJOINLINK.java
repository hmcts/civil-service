package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.EnumSet;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code SEND_CVP_JOIN_LINK} for case type {@code CIVIL}.
 */
@Component
public class SENDCVPJOINLINK implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String SEND_CVP_JOIN_LINK = "SEND_CVP_JOIN_LINK";

    /**
     * Registers event {@code SEND_CVP_JOIN_LINK}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        var fields = builder.event(SEND_CVP_JOIN_LINK)
            .forStateTransition(EnumSet.allOf(CaseState.class), CaseState.PREPARE_FOR_HEARING_CONDUCT_HEARING)
            .name("Send Join Link")
            .description("Triggered when hearing is in an EXCEPTION state")
            .displayOrder(1)
            .showSummary()
            .endButtonLabel("Submit")
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CASEWORKER_CIVIL_SYSTEMUPDATE)
            .fields();
        fields.page("FeatureToggleWA");
        fields.readonlyNoSummary(CaseData::getFeatureToggleWA);
    }
}
