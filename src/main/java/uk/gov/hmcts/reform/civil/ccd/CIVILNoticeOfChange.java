package uk.gov.hmcts.reform.civil.ccd;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>The Notice of Change challenge questions for {@code CIVIL}.
 */
@Component
public class CIVILNoticeOfChange implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * Configures the Notice of Change challenge questions for {@code CIVIL}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        builder.noticeOfChange()
            .challenge("NoCChallenge")
                .question("clientName", "Your Client's name as it appears as written on the case.")
                .displayOrder(1)
                .answerFieldType("Text")
                .answer(UserRole.APPLICANTSOLICITORONE)
                    .field("applicant1.partyName}|${applicant2.partyName")
                .answer(UserRole.RESPONDENTSOLICITORONE)
                    .field("respondent1.partyName")
                .answer(UserRole.RESPONDENTSOLICITORTWO)
                    .field("respondent2.partyName")
                .done();
    }
}
