package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.ccd.event.page.ACKNOWLEDGECLAIMProdConfirmNameAddressPage;
import uk.gov.hmcts.reform.civil.ccd.event.page.ACKNOWLEDGECLAIMProdResponseIntentionPage;
import uk.gov.hmcts.reform.civil.ccd.event.page.ACKNOWLEDGECLAIMProdSingleResponsePage;
import uk.gov.hmcts.reform.civil.ccd.event.page.ACKNOWLEDGECLAIMProdSolicitorReferencesPage;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.EnvironmentFlags;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code ACKNOWLEDGE_CLAIM} for case type {@code CIVIL}.
 */
@Component
public class ACKNOWLEDGECLAIMProd implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String ACKNOWLEDGE_CLAIM = "ACKNOWLEDGE_CLAIM";

    /**
     * Registers overlay event {@code ACKNOWLEDGE_CLAIM} (active when CCD_DEF_ENV=prod).
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        if (!EnvironmentFlags.flag("CCD_DEF_ENV", "prod")) return;
        var fields = builder.event(ACKNOWLEDGE_CLAIM)
            .forState(CaseState.AWAITING_RESPONDENT_ACKNOWLEDGEMENT)
            .name("Acknowledge claim")
            .description("Defendant solicitor is acknowledging claim")
            .displayOrder(4)
            .showSummary()
            .endButtonLabel("Acknowledge claim")
            .explicitGrants()
            .grant(Permission.CRU, UserRole.RES_SOL_ONE_UNSPEC_PROFILE, UserRole.RES_SOL_TWO_UNSPEC_PROFILE)
            .grant(Set.of(Permission.R), UserRole.APP_SOL_UNSPEC_PROFILE, UserRole.CASEWORKER_CIVIL_ADMIN, UserRole.CASEWORKER_CIVIL_STAFF, UserRole.JUDGE_PROFILE, UserRole.LEGAL_ADVISER)
            .fields();
        ACKNOWLEDGECLAIMProdConfirmNameAddressPage.apply(fields);
        ACKNOWLEDGECLAIMProdSingleResponsePage.apply(fields);
        ACKNOWLEDGECLAIMProdResponseIntentionPage.apply(fields);
        ACKNOWLEDGECLAIMProdSolicitorReferencesPage.apply(fields);
    }
}
