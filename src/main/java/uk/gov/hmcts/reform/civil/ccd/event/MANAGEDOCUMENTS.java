package uk.gov.hmcts.reform.civil.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.civil.enums.CaseState;
import uk.gov.hmcts.reform.civil.model.CaseData;
import uk.gov.hmcts.reform.civil.model.StaffDocument;
import uk.gov.hmcts.reform.civil.model.UserRole;

/**
 * Generated by ccd-definition-converter from CIVIL on migration; owned by this service team.
 *
 * <p>Event {@code MANAGE_DOCUMENTS} for case type {@code CIVIL}.
 */
@Component
public class MANAGEDOCUMENTS implements CCDConfig<CaseData, CaseState, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String MANAGE_DOCUMENTS = "MANAGE_DOCUMENTS";

    /**
     * Registers event {@code MANAGE_DOCUMENTS}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, CaseState, UserRole> builder) {
        var fields = builder.event(MANAGE_DOCUMENTS)
            .forAllStates()
            .name("Manage Documents")
            .displayOrder(4)
            .showSummary()
            .showEventNotes()
            .endButtonLabel("Submit")
            .explicitGrants()
            .grant(Set.of(Permission.R), UserRole.APP_SOL_SPEC_PROFILE, UserRole.CITIZEN_CLAIMANT_PROFILE, UserRole.CITIZEN_DEFENDANT_PROFILE, UserRole.RES_SOL_ONE_SPEC_PROFILE, UserRole.RES_SOL_TWO_SPEC_PROFILE)
            .grant(Permission.CRU, UserRole.CASEWORKER_CIVIL_ADMIN, UserRole.CASEWORKER_CIVIL_RPAROBOT, UserRole.CUI_ADMIN_PROFILE, UserRole.JUDGE_PROFILE)
            .fields();
        fields.page("ManageDocuments");
        fields.complex(CaseData::getManageDocuments).done()
                    .caseEventFieldLabel("Bulk scanned or emailed documents");
        fields.complex(CaseData::getManageDocuments, StaffDocument.class)
                    .mandatory(StaffDocument::getDocumentName)
                    .eventLabel("Document Name")
                    .mandatory(StaffDocument::getDocumentType)
                    .eventLabel("Document Type")
                    .optional(StaffDocument::getDocumentTypeOther)
                    .eventLabel("What type of document is it?")
                    .mandatory(StaffDocument::getDocumentLink)
                    .eventLabel("Upload essential document").done();
        fields.readonlyNoSummary(CaseData::getCaseTitleWithReference);
    }
}
